1.请说明标准I/O函数的2个优点。它为何拥有这2个优点
答：1.良好的移植性。2.可以利用缓冲区提高性能。
    标准函数按照ANSI C标准定义，可以支持所有操作系统，所有具有良好的移植性。
    I/O函数添加了缓冲区之后，每次传输的数据量会增多，传输的次数会减少，这样就可以较少数据头等无关数据的发送，同时减少数据头的合成，
    也可以缩短发送的总时间。
2.“调用fputs函数传输数据时，调用后应立即开始发送”，为何上述想法是错误的？为了达到这种效果应添加哪些处理过程？
答：调用fputs函数之后，数据会被写入缓冲区，不会立即发送。添加fflush函数，即可完成立即发送。
