3. 在浏览器地址栏输入www.orentec.co.kr，并整理出主页显示过程。假设浏览器访问的默认DNS服务器中并没有关于www.orentec.co.kr的IP地址信息
答：由于默认的DNS服务器中没有IP地址信息，主机会向上一级的DNS服务器请求，这样一级一级传递，到达根服务器。根服务器知道该向哪个DNS服务器请求，
    由根目录一级一级向下传递，找到指定的IP信息后原路返回给主机。
